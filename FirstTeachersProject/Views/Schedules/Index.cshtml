@model IEnumerable<FirstTeachersProject.Models.Schedule>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Child.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Mailing.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateToMail)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateMailed)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Child.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Mailing.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateToMail)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateMailed)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
